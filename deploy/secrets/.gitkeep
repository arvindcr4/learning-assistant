# This directory is for storing generated secrets during deployment
# Files in this directory should NOT be committed to version control
# The .gitkeep file allows the directory to be tracked while keeping it empty